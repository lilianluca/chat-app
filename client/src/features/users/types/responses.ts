export interface UserResponse {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  bio: string;
  statusEmoji: string;
}

export interface UpdateProfileResponse {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  bio: string;
  statusEmoji: string;
}
